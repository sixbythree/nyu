int i=0;
bool pressed = false;
bool pressing = false;


void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600);
  pinMode(2,INPUT);
  
  
  
}

void loop() {
  // put your main code here, to run repeatedly:
  
  int buttonState = digitalRead(2);
  if(buttonState){
    if(pressing==false){
      Serial.print("Pressing");
      Serial.print("\n");
      pressed = !pressed;
      Serial.print("pressed state: ");
    }
    pressing=true;
  } else{
    pressing=false;
    }
  

  if(pressed){
    Serial.print("Math question: ");
    Serial.print("\n");
    i+=1;
  }

  //Serial.print(buttonState);
  //Serial.print("\n");

}
